<h1>タスク一覧</h1>

<ul>
    <% @tasks.each do |task| %>
        <il><%= link_to task.id, task_path(task)%> : <%= task.status %> &gt; <%= task.content %> </il>
    <% end %>
</ul>

<%= link_to "新規タスク作成", new_task_path%>